package lavender

import java.nio.file.Path

import lavender.util.SnowflakeMap

object Lavender {
  val environment = LvEnvironment(SnowflakeMap.empty, SnowflakeMap.empty)

  def init(): Unit = {

  }

  def readFile(path: Path) = {

  }
}
